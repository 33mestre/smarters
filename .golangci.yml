run:
  timeout: 5m
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - structcheck
    - varcheck
    - ineffassign
    - gofmt
